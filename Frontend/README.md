
  # Build Animated Workout Tracker

  This is a code bundle for Build Animated Workout Tracker. The original project is available at https://www.figma.com/design/j97gtafTCCkJpbPa2zLWxs/Build-Animated-Workout-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  